import { setActivePinia, createPinia } from "pinia";
import { describe, it, expect, beforeEach } from "vitest";
// import { useInvoiceStore } from "@/stores/invoice";
// const store = useInvoiceStore();
describe("invoice store", () => {
	beforeEach(() => {
		setActivePinia(createPinia());
	});

	it('should be invalid when empty', () => {
		//add test
	});
	it('should return value for payment term', () => {
		//add test
	});
	it('should calculate due date based on payment term', () => {
		//add test
	});
	it('should calculate due date based on payment term', () => {
		//add test
	});
	it('should be unique Item name', () => {
		//add test
	});
	it('should create a new empty row in item list', () => {
		//add test
	});
	it('should return correct item price total', () => {
		//add test
	});
	it('should return correct itemlist price total', () => {
		//add test
	});
	it('should remove item from item list', () => {
		//add test
	});





});
